import { INCIDENT_CATEGORY_IDS } from "../constants/incident-categories.js";

const categoryLines = [
  "fight — драка, физическая агрессия",
  "weapon — оружие или предмет, похожий на оружие",
  "fall — падение человека",
  "smoking — курение, пар от сигарет/вейпа",
  "phone_usage — использование телефона (ученик/запретная зона)",
  "sleep — сон за партой, голова на столе",
  "lost_property — забытые вещи без присмотра",
  "crowd — сбор людей, скопление, толпа",
  "wanted_person — разыскиваемое/подозрительное лицо, посторонний в запрещённой зоне",
  "fence_climbing — перелезание через забор/ограждение",
  "anpr — распознавание госномера (ГРНЗ) на территории",
  "fire — открытое пламя, возгорание",
  "smoke — задымление, дым без явного пламени",
].join("\n- ");

const enumJson = INCIDENT_CATEGORY_IDS.map((id) => `"${id}"`).join(" | ");

export const incidentAnalyzeSystemPrompt = `Ты — система видеоаналитики безопасности школы Qoz Vision.
Проанализируй видеозапись и определи наиболее подходящий тип инцидента.

Допустимые категории (ровно одна, поле "category"):
- ${categoryLines}

Ответь ТОЛЬКО валидным JSON без markdown:
{
  "category": ${enumJson},
  "confidence": <число 0-100>,
  "description": "<краткое описание на русском, 1-3 предложения>"
}

Если явного нарушения нет, выбери crowd с confidence ниже 35 и опиши нейтральную обстановку.
Не выдумывай детали, которых не видно на видео.`;
